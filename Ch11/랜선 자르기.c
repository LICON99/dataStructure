/*랜선 자르기

시간 제한	메모리 제한	제출	정답	맞힌 사람	정답 비율
2 초	128 MB	271857	66558	44935	22.054%
문제
집에서 시간을 보내던 오영식은 박성원의 부름을 받고 급히 달려왔다. 박성원이 캠프 때 쓸 N개의 랜선을 만들어야 하는데 너무 바빠서 영식이에게 도움을 청했다.

이미 오영식은 자체적으로 K개의 랜선을 가지고 있다. 그러나 K개의 랜선은 길이가 제각각이다. 박성원은 랜선을 모두 N개의 같은 길이의 랜선으로 만들고 싶었기 때문에 K개의 랜선을 잘라서 만들어야 한다. 예를 들어 300cm 짜리 랜선에서 140cm 짜리 랜선을 두 개 잘라내면 20cm는 버려야 한다. (이미 자른 랜선은 붙일 수 없다.)

편의를 위해 랜선을 자르거나 만들 때 손실되는 길이는 없다고 가정하며, 기존의 K개의 랜선으로 N개의 랜선을 만들 수 없는 경우는 없다고 가정하자. 그리고 자를 때는 항상 센티미터 단위로 정수길이만큼 자른다고 가정하자. N개보다 많이 만드는 것도 N개를 만드는 것에 포함된다. 이때 만들 수 있는 최대 랜선의 길이를 구하는 프로그램을 작성하시오.

입력
첫째 줄에는 오영식이 이미 가지고 있는 랜선의 개수 K, 그리고 필요한 랜선의 개수 N이 입력된다. K는 1이상 10,000이하의 정수이고, N은 1이상 1,000,000이하의 정수이다. 그리고 항상 K ≦ N 이다. 그 후 K줄에 걸쳐 이미 가지고 있는 각 랜선의 길이가 센티미터 단위의 정수로 입력된다. 랜선의 길이는 231-1보다 작거나 같은 자연수이다.

출력
첫째 줄에 N개를 만들 수 있는 랜선의 최대 길이를 센티미터 단위의 정수로 출력한다.

예제 입력 1
4 11
802
743
457
539
예제 출력 1
200*/

#define _CRT_SECURE_NO_WARNINGS // scanf 보안 경고 무시 (Visual Studio용)
#include <stdio.h>
#include <stdlib.h>

int main()
{
	int N, K, i, j;
	long long k_max = 0, max_len = 0, cnt;
	long long *list = NULL;

	scanf("%d %d", &K, &N); // K: 현재 가지고 있는 랜선 개수, N: 필요한 랜선 개수

	list = (long long *)calloc(K, sizeof(long long));
	// K개의 랜선 길이를 저장할 배열 동적 할당 (모두 0으로 초기화)

	for (i = 0; i < K; i++)
	{
		scanf(" %lld", &list[i]); // 각 랜선 길이 입력 받기
		if (list[i] > k_max)	  // 가장 긴 랜선을 찾아 k_max에 저장
			k_max = list[i];
	}

	long long left, mid, right;
	left = 1;	   // 이진 탐색의 시작 길이: 최소 1cm
	right = k_max; // 가장 긴 랜선 길이로 초기화

	while (left <= right)
	{
		mid = (left + right) / 2; // 중간 길이(mid)를 기준으로 자를 것인지 판단
		// mid 길이로 잘랐을 때 얻을 수 있는 랜선 개수 계산
		for (j = 0, cnt = 0; j < K; j++)
		{
			cnt += list[j] / mid; // 각 랜선을 mid 길이로 잘라 몇 개 나오는지 누적
		}

		// 목표 개수 N 이상을 만들 수 있고, 이 때 길이(mid)가 현재까지 저장된 max_len보다 크면 갱신
		if (N <= cnt && max_len < mid)
			max_len = mid;

		// 자른 개수가 부족하면 mid 길이가 너무 김 → 길이를 줄인다
		if (cnt < N)
			right = mid - 1;
		else
			left = mid + 1; // 충분하거나 넘치면 mid를 늘려서 더 긴 길이를 시도
	}

	printf("%lld\n", max_len); // 만들 수 있는 랜선의 최대 길이 출력

	return 0;
}